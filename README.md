# cluster_sim Repository

This repository contains the cluster_sim code. It simulates a stellar object using a define radial density model.

It also contains the mcmc_density.py code to fit a density model